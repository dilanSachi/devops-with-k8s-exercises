apiVersion: v1
kind: ConfigMap
metadata:
  name: ex207-config-map
  namespace: exercises
data:
  MESSAGE: "hello world"
  information.txt: |-
    this text is from file
  init-db.sql: |-
    create table PINGPONGCOUNTER
    (
        id      int,
        counter int
    );
    insert into PINGPONGCOUNTER (id, counter)
    values (1, 0);
  DB_USER: "postgres"
  DB_PASSWORD: "postgres_pass"
  DB_URL: "ex207-postgres-svc/"
