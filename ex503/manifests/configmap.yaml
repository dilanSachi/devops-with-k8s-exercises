apiVersion: v1
kind: ConfigMap
metadata:
  name: ex503-config-map
  namespace: exercises
data:
  MESSAGE: "hello world"
  information.txt: |-
    this text is from file
  PING_PONG_APP_URL: "http://ex503-gateway-istio:80"
  GREETER_APP_URL: "http://ex503-gateway-istio:80"
  init-db.sql: |-
    create table PINGPONGCOUNTER
    (
        id      int,
        counter int
    );
    insert into PINGPONGCOUNTER (id, counter)
    values (1, 0);
  DB_USER: "postgres"
  DB_PASSWORD: "postgres_pass"
  DB_URL: "ex503-postgres-svc/"
