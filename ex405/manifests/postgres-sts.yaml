apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ex405-postgres-stset
  namespace: project
spec:
  serviceName: ex405-postgres-svc
  replicas: 1
  selector:
    matchLabels:
      app: ex405-postgresapp
  template:
    metadata:
      labels:
        app: ex405-postgresapp
    spec:
      containers:
        - name: postgres
          image: postgres:14.20-alpine3.23
          ports:
            - name: dbport
              containerPort: 5432
          volumeMounts:
            - name: postgres-data-storage
              mountPath: /var/lib/postgresql/data
              subPath: postgres
            - name: init-vol
              mountPath: "/docker-entrypoint-initdb.d/"
              readOnly: true
          env:
            - name: POSTGRES_PASSWORD
              value: "postgres_pass"
          resources:
            limits:
              cpu: "150m"
              memory: "250Mi"
            requests:
              cpu: "50m"
              memory: "100Mi"
      volumes:
        - name: init-vol
          configMap:
            name: ex405-config-map
            items:
              - key: "init-db.sql"
                path: "init-db.sql"
  volumeClaimTemplates:
    - metadata:
        name: postgres-data-storage
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 100Mi
---
apiVersion: v1
kind: Service
metadata:
  name: ex405-postgres-svc
  namespace: project
  labels:
    app: ex405-postgres
spec:
  ports:
    - port: 5432
      name: dbport
      protocol: TCP
      targetPort: 5432
  clusterIP: None
  selector:
    app: ex405-postgresapp
