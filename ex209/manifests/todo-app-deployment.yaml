apiVersion: apps/v1
kind: Deployment
metadata:
  name: ex209-todo-app
  namespace: project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ex209-todo-app
  template:
    metadata:
      labels:
        app: ex209-todo-app
    spec:
      volumes:
        - name: shared-volume
          persistentVolumeClaim:
            claimName: ex209-vol-claim
      containers:
        - name: ex209-todo-app
          image: dilansachi/devopswk8s-ex209-todo-app:1.0.0
          imagePullPolicy: IfNotPresent
          env:
            - name: PORT
              value: "3002"
            - name: IMAGE_FILE_PATH
              valueFrom:
                configMapKeyRef:
                  name: ex209-config-map
                  key: image_file_path
            - name: IMAGE_RESOURCE_URL
              valueFrom:
                configMapKeyRef:
                  name: ex209-config-map
                  key: image_resource_url
            - name: BACKEND_URL
              valueFrom:
                configMapKeyRef:
                  name: ex209-config-map
                  key: backend_url
          volumeMounts:
            - name: shared-volume
              mountPath: /usr/src/app/files
