apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ex208-vol-claim
  namespace: project
spec:
  storageClassName: ex208-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
