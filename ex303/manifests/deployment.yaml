apiVersion: apps/v1
kind: Deployment
metadata:
  name: ex303
  namespace: exercises
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ex303
  template:
    metadata:
      labels:
        app: ex303
    spec:
      containers:
        - name: ex303-ping-pong-application
          image: dilansachi/devopswk8s-ex303-ping-pong-application:latest
          imagePullPolicy: Always
          env:
            - name: PORT
              value: "3003"
            - name: DB_USER
              valueFrom:
                configMapKeyRef:
                  name: ex303-config-map
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: ex303-config-map
                  key: DB_PASSWORD
            - name: DB_URL
              valueFrom:
                configMapKeyRef:
                  name: ex303-config-map
                  key: DB_URL
        - name: ex303-log-reader
          image: dilansachi/devopswk8s-ex303-log-reader:latest
          imagePullPolicy: Always
          env:
            - name: PORT
              value: "3002"
            - name: MESSAGE
              valueFrom:
                configMapKeyRef:
                  name: ex303-config-map
                  key: MESSAGE
            - name: PING_PONG_APP_URL
              valueFrom:
                configMapKeyRef:
                  name: ex303-config-map
                  key: PING_PONG_APP_URL
          volumeMounts:
            - name: config-vol
              mountPath: "/config-vol"
              readOnly: true
      volumes:
        - name: config-vol
          configMap:
            name: ex303-config-map
            items:
              - key: "information.txt"
                path: "information.txt"
