apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ex107-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ex107-log-output-svc
                port:
                  number: 2345
