apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ex207-vol-claim
  namespace: exercises
spec:
  storageClassName: ex207-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
