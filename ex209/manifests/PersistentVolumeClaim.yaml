apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ex209-vol-claim
  namespace: project
spec:
  storageClassName: ex209-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
