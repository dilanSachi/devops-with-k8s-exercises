apiVersion: v1
kind: Service
metadata:
  name: ex301-log-app-svc
  namespace: exercises
spec:
  type: LoadBalancer
  selector:
    app: ex301-log-app
  ports:
    - name: log-app
      port: 2345
      protocol: TCP
      targetPort: 3002
