apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ex113-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ex113-svc
                port:
                  number: 2345
