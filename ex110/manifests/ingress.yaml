apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ex110-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ex110-svc
                port:
                  number: 2345
