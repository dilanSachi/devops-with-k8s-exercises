apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: ex311-dep-pdb
  namespace: project
spec:
  maxUnavailable: 50%
  selector:
    matchExpressions:
      - key: app
        operator: In
        values: [ "ex311-todo-backend", "ex311-todo-app" ]
