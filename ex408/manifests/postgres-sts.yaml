apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ex408-postgres-stset
  namespace: project
spec:
  serviceName: ex408-postgres-svc
  replicas: 1
  selector:
    matchLabels:
      app: ex408-postgresapp
  template:
    metadata:
      labels:
        app: ex408-postgresapp
    spec:
      containers:
        - name: postgres
          image: postgres:14.20-alpine3.23
          ports:
            - name: dbport
              containerPort: 5432
          volumeMounts:
            - name: postgres-data-storage
              mountPath: /var/lib/postgresql/data
              subPath: postgres
            - name: init-vol
              mountPath: "/docker-entrypoint-initdb.d/"
              readOnly: true
          env:
            - name: POSTGRES_PASSWORD
              value: "postgres_pass"
          resources:
            limits:
              cpu: "150m"
              memory: "250Mi"
            requests:
              cpu: "50m"
              memory: "100Mi"
      volumes:
        - name: init-vol
          configMap:
            name: ex408-config-map
            items:
              - key: "init-db.sql"
                path: "init-db.sql"
  volumeClaimTemplates:
    - metadata:
        name: postgres-data-storage
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 100Mi
---
apiVersion: v1
kind: Service
metadata:
  name: ex408-postgres-svc
  namespace: project
  labels:
    app: ex408-postgres
spec:
  ports:
    - port: 5432
      name: dbport
      protocol: TCP
      targetPort: 5432
  clusterIP: None
  selector:
    app: ex408-postgresapp
