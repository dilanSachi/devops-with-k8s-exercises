apiVersion: apps/v1
kind: Deployment
metadata:
  name: ex205-log-app
  namespace: exercises
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ex205-log-app
  template:
    metadata:
      labels:
        app: ex205-log-app
    spec:
      volumes:
        - name: shared-volume
          persistentVolumeClaim:
            claimName: ex205-vol-claim
        - name: config-vol
          configMap:
            name: config-map
            items:
              - key: "information.txt"
                path: "information.txt"
      containers:
        - name: ex205-log-reader
          image: dilansachi/devopswk8s-ex205-log-reader:1.0.0
          imagePullPolicy: IfNotPresent
          env:
            - name: PORT
              value: "3002"
            - name: MESSAGE
              valueFrom:
                configMapKeyRef:
                  name: config-map
                  key: MESSAGE
            - name: PING_PONG_URL
              valueFrom:
                configMapKeyRef:
                  name: config-map
                  key: PING_PONG_URL
          volumeMounts:
            - name: shared-volume
              mountPath: /usr/src/app/files
            - name: config-vol
              mountPath: "/config-vol"
              readOnly: true
        - name: ex205-log-writer
          image: dilansachi/devopswk8s-ex205-log-writer:1.0.0
          imagePullPolicy: IfNotPresent
          volumeMounts:
            - name: shared-volume
              mountPath: /usr/src/app/files
