apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ex108-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ex108-web-server-svc
                port:
                  number: 2345
